sudo: false

language: java
jdk:
  - oraclejdk8
  
before_install:
  - git clone https://github.com/Valkryst/VRadio.git
  
script: "mvn cobertura:cobertura"

after_success:
  - bash <(curl -s https://codecov.io/bash)

notification:
  email:
    on_success: never
    on_failure: always
